# URL
https://hackmyvm.eu/challenges/challenge.php?c=010
# Concept
* base85 encoding
# Method of solve
* this looks kind of like base64 encoding, but it includes special characters that are not part of the b64 character set
* this is `base85`, which includes all of the special ASCII characters along with the base64 set
* we can CyberChef to decode the message
