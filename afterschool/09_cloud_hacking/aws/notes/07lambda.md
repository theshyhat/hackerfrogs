# Lambda Overview
Lambda is a service which allows users to upload code an run it on AWS servers
# Enumeration
## List functions
```
aws lambda list-functions --region us-east-1
```
## List function details
```
aws lambda get-function --function-name <function-name> --region us-east-1
```







